<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mi_proyectofinal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/santa"
        android:label="@string/app_name"
        android:roundIcon="@drawable/santa"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CorosActivity"></activity>
        <activity android:name=".AlabanzasActivity" />
        <activity android:name=".Alabanzas" />
        <activity android:name=".registro_coros" />
        <activity android:name=".registro_alabanzas" />
        <activity android:name=".SplashScreen"
            android:theme="@style/AppTheme.No">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>